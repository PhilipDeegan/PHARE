property:
  cxx.flags: >-
    --std=c++20 -fsized-deallocation -fPIC

  lnk.flags: -pthread
# local:
#   repo: /mkn/r
#   mod-repo: /mkn/m
# remote:
#   repo: git@github.com:mkn/
#   mod-repo: git@github.com:mkn-mod/
# env: |
#   PATH=/opt/rocm/bin:${PATH}
compiler:
  mask:
    g++:
      icpx
file:
  - type: cpp:cxx:cc:c:S
    archiver: ar -cr
    compiler: icpx ${cxx.flags}
    linker: icpx ${lnk.flags}
